<!DOCTYPE html>
<html lang="en">
	<head>
		<%- include('layouts/head');-%>
		<!-- -->
		<!-- body tag below -->
		<!-- -->
		<title>Updates</title>
	</head>

	<body>
		<%- include('layouts/nav');-%> <%- include('layouts/script');-%> <% for (var
		i = 0; i < posts.length ; i++){%>
		<div class="card">
			<div class="card-body">
				<h5 class="card-title"><%= posts[i].title %></h5>
				<p class="card-text"><%= posts[i].body %></p>
			</div>
		</div>
		<%}%>
	</body>
	<!-- -->
	<!-- body tag above -->
	<!-- -->
</html>
